{
  "name": "cookiesjs",
  "version": "4.0.0",
  "description": "Intuitive cookie manipulation library for the front-end",
  "repository": "github:franciscop/cookies",
  "funding": "https://www.paypal.me/franciscopresencia/19",
  "author": "Francisco Presencia <public@francisco.io> (https://francisco.io/)",
  "license": "MIT",
  "type": "module",
  "main": "index.min.js",
  "files": [
    "index.min.js",
    "index.d.ts"
  ],
  "types": "index.d.ts",
  "scripts": {
    "build": "bun run build:bundle && bun run build:types",
    "build:bundle": "esbuild src/index.ts --bundle --format=esm --minify --outfile=index.min.js",
    "build:types": "dts-bundle-generator --out-file index.d.ts --project tsconfig.json src/index.ts",
    "lint": "tsc",
    "size": "echo $(gzip -c index.min.js | wc -c) bytes",
    "start": "bun test --watch",
    "test": "bun test"
  },
  "keywords": [
    "cookies",
    "js",
    "javascript",
    "frontend",
    "expiration",
    "secure"
  ],
  "devDependencies": {
    "@happy-dom/global-registrator": "^20.9.0",
    "@types/bun": "^1.3.0",
    "dts-bundle-generator": "^9.5.1",
    "esbuild": "^0.28.0",
    "typescript": "^6.0.0"
  }
}
